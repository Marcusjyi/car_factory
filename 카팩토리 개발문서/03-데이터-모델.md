# 03. 데이터 모델

최종 갱신: 2026-08-04  
근거: `firestore.rules`, `apps/store/src/types/*`, `functions/src/**`

## 컬렉션 요약

| 경로 | 클라이언트 | 용도 |
|------|------------|------|
| `users/{uid}` | 본인 R/W (role/status 변경 불가) | 회원 프로필 |
| `publicProfiles/{uid}` | 공개 읽기 / 본인 displayName | 판매자 공개명 |
| `products/{productId}` | 상태·판매자 규칙에 따른 R/W | 상품 |
| `users/{uid}/favorites/{productId}` | 본인 | 찜 |
| `users/{uid}/addresses/{addressId}` | 본인 | 배송지 |
| `users/{uid}/cartItems/{productId}` | 본인 | 장바구니 |
| `users/{uid}/counters/{id}` | 본인 읽기 / **쓰기 금지** | 예: chatUnread |
| `users/{uid}/fcmTokens/{tokenId}` | 본인 | FCM 디바이스 토큰 |
| `orders/{orderId}` | 당사자·어드민 읽기 / **쓰기 금지** | 직거래 주문 |
| `payments/{paymentId}` | 읽기만 / 쓰기 금지 | PG 자리(미사용) |
| `chatRooms/{roomId}` | 참여자 읽기 / **메타 쓰기 금지** | 채팅방 |
| `chatRooms/{roomId}/messages/{id}` | 조건 충족 시 create | 메시지 |
| `supportInquiries/{id}` | 본인 create·read / 어드민 update | CS |
| `adminAuditLogs/{id}` | 어드민 읽기 / 쓰기 금지 | 감사 |
| `admins/{uid}` | 본인·어드민 읽기 / **쓰기 금지** | 운영 계정 |
| `partners/{partnerId}` | 공개 읽기 / **어드민만 쓰기** | 장착 파트너스 |
| `productListingCounters/{dateKey}` | 금지 | 리스팅 번호 |
| `orderCounters/{dateKey}` | 금지 | 주문 번호 |

문서 ID: 회원·어드민 = Auth UID. 채팅방 = `{productId}_{buyerUid}`.

---

## Partner (장착 파트너스)

**컬렉션:** `partners/{partnerId}` (웹 경로 `/installers/{id}` 와 ID 공유)

| 필드 | 타입 | 설명 |
|------|------|------|
| name | string | 지점명 |
| region | string | 권역 표시 |
| address | string | 주소 |
| phone | string | 전화 |
| hours | string | 영업시간 |
| description | string | 소개 |
| specialties | string[] | 전문분야 태그 |
| specialtyLabel | string? | 앱 카드 한 줄 전문 문구 |
| badges | string[] | 뱃지 (당일 예약 가능 등) |
| photoURL | string | 대표 사진 URL |
| photoPath | string? | Storage 경로 |
| ratingAverage / ratingCount | number? | 별점 |
| isFeatured | bool | 앱 홈 노출 |
| displayOrder | number | 목록 정렬 |
| isActive | bool | 공개 여부 |

---

## Product

**파일:** `apps/store/src/types/product.ts`

| 필드 그룹 | 값 |
|-----------|-----|
| status | `draft` \| `selling` \| `reserved` \| `sold` \| `hidden` \| `blocked` |
| condition | `new` \| `used` \| `refurbished` |
| grade | `A` \| `B` \| `C` |
| delivery | `parcel` \| `direct` |

중요 필드: `sellerUid`, `title`, `partName`, `partNumber`, 차량 정보, `price`, `images[]`, `thumbnailURL`, `listingNumber`(`CF-YYMMDD-NNNNN`), `searchKeywords`, `reservedBuyerUid`, `activeOrderId`, `soldAt`.

규칙 요약:

- 생성 시 클라이언트는 `status: draft`만, **`listingNumber` 설정 금지**.
- `reserved` 전환은 Functions만.
- 예약중이면 수정·삭제·단독 판매완료 제한.

---

## Order (직거래)

**파일:** `apps/store/src/types/trade.ts`  
**로직:** `functions/src/trades/direct-trade.ts`

| 필드 | 값 |
|------|-----|
| status | `reserved` \| `completed` \| `cancelled` |
| dealMethod | `meet` \| `transfer` |
| fulfillmentMethod | `meetup` \| `delivery` |
| paymentProvider | `"direct"` (MVP) |
| orderNumber | `CF-T-YYMMDD-NNNNN` |

기타: buyer/seller uid·표시명, `productSnapshot`, 금액, `transferInfo`, `shippingInfo`, 히스토리 hide 플래그.

---

## Chat

| 항목 | 값 |
|------|-----|
| room status | `active` \| `closed` \| `blocked` |
| message type | `text` \| `image` \| `system` |
| roomId | `{productId}_{buyerUid}` |
| chatOpenUntil | 판매완료 후 약 30일 (`POST_SALE_CHAT_DAYS`) |

메시지 create: room `active` 이고 `chatOpenUntil` 이전.

---

## User

| 필드 | 값 |
|------|-----|
| role | `user` \| `admin` \| `super_admin` (스토어 프로필; **운영 게이트는 admins**) |
| status | `active` \| `suspended` \| `withdrawn` |

기타: `defaultTransferAccount`, `shippingAddress`, `tradeStats`, `profileCompleted`.

---

## Admin

**문서:** `카팩토리 개발문서/ADMIN_SCHEMA.md`  
컬렉션: **`admins/{uid}`** (단수 `admin` 아님)  
생성: **CLI만** — `car_factory_admin` 에서 `npm run create-admin` (`scripts/create-admin.cjs`). 어드민 UI 추가 금지.

| 필드 | 타입 |
|------|------|
| email | string |
| displayName | string |
| employeeId | string? |
| isApproved | boolean (`true`여야 로그인) |
| role | `admin` \| `super_admin` |
| createdAt / updatedAt | timestamp |

비밀번호는 Firestore에 두지 않음. Auth Email/Password만.

---

## Storage 경로

| 경로 | 규칙 |
|------|------|
| `product-images/{uid}/{productId}/{file}` | 공개 읽기, 소유자 쓰기 ≤10MB jpeg/png/webp |
| `profile-images/{uid}/{file}` | 동일 |
| `partner-images/{partnerId}/{file}` | 공개 읽기, **어드민** 쓰기 ≤10MB jpeg/png/webp |
| `support-files/{uid}/{inquiryId}/{file}` | 소유자 |
| `chat-images/...` | **전체 deny** (미구현) |

스토어 업로드 관례: `{imageId}_{thumb\|list\|detail}.webp` (약 400 / 800 / 1600).  
구현: `apps/store/src/features/products/image-upload.ts`.

---

## Cloud Functions (export 기준)

| 이름 | 역할 |
|------|------|
| `onProductCreatedAssignListingNumber` | 리스팅 번호 부여 |
| `getOrCreateChatRoom` | 채팅방 생성/재사용 |
| `markChatRoomRead` | 읽음·미읽음 카운터 |
| `onMessageCreated` | lastMessage / unread / **FCM 푸시** |
| `createDirectTrade` | 예약 + 주문 |
| `cancelDirectTrade` | 예약 취소 → selling |
| `completeDirectTrade` | 판매완료 → sold |
| `setTradeTransferInfo` | 계좌 정보 |
| `hideDirectTradeFromHistory` | 히스토리 숨김 |
| `seedInitialAdmin` / `createAdminUser` / `listAdmins` / `setAdminRole` / `revokeAdminAccess` | 관리자 |
| `seedPartners` | 장착점 → `partners` 시드 |

새 callable을 추가하면 `functions/src/index.ts` export + 본 문서 + Rules 영향을 함께 갱신한다.
